SELECT S.meeting_days, S.beginning_time, S.ending_time FROM section S;
       -- INNER JOIN (SELECT age, count(age) as cnt
                     -- FROM Students 
                    -- GROUP BY age) C ON S.professor = C.
